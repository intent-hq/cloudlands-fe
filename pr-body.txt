feat: rewire RTK toggle to daemon rtk.enabled setting

Rewires the RTK settings toggle to use the daemon's rtk.enabled setting instead of FE-local persistence, completing Wave 2 of the RTK optimization restore (Wave 1: intentd PR #190, merged as 5c5fb75).

Changes:
- RtkSettings.svelte: Read/write rtk.enabled via appClient.settings.get/update (daemon-backed)
- app-settings-schema.ts: Mark rtk.enabled as daemon-settings source
- Removed dead injection path: Deleted rtk-detector.ts and its test
- Test: Added RtkSettings.test.ts with 9 comprehensive tests including error handling
- User-visible error feedback for settings operations
- Split error handling to avoid masking failures
- Handle null return from settings.get

All gates passed:
✅ pnpm run check — PASS
✅ All tsc --noEmit — PASS
✅ pnpm vitest run RtkSettings.test.ts — PASS (9/9 tests)

Reviewed and addressed feedback from panghy and Copilot (PR #89)
