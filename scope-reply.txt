Out of scope for STAB-20. This task specifically targets sidebar pinned workspaces not surviving app restart. Per-workspace localStorage keys (MULTISELECT_SIDEBAR_SELECTED_TABS_PREFIX, WORKSPACE_NOTE_ORDER_PREFIX, WORKSPACE_COLLAPSED_NOTES_PREFIX) are a separate concern and should be addressed in a follow-up STAB issue if needed. The middleware currently hydrates these maps on boot but doesn't persist updates - this is intentional scope limitation for this task.
