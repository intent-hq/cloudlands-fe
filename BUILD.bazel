load("//tools/bzl:trivy.bzl", "trivy_test")

trivy_test(
    name = "pnpm-lock.trivy_test",
    src = "pnpm-lock.yaml",
    mode = "fs",
    tags = [
        "lint",
        "no-cache",
        "no-ci",
    ],
)
