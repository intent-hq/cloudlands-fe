chore(fe): remove observability feature and drop better-sqlite3

Strip the Electron-side observability feature and its only native
dependency (better-sqlite3), per the intentd-owns-the-data decision.

- Delete src/features/observability/ (event-collector, observability-service,
  event-collector-client, event-storage, observability.ipc, terminal-ui,
  web-dashboard) and the associated /observability route + API
- Remove OBSERVABILITY entries from the IPC registry, channel exports, and
  preload allowlists; regenerate src/preload/index.ts
- Drop better-sqlite3 + @types/better-sqlite3, including the
  onlyBuiltDependencies entry, electron-builder asarUnpack rule,
  dev-launcher native-module check, postinstall and ensure-native-deps
  rebuild flows, and the rebuild:electron / rebuild:node scripts
- Remove eventCollector / AgentEventType tracking from agent-stream-lifecycle
  (renderer keeps workspace-metrics tracking); update integration test mocks
- Trim logging-config (drop EventStorage entry) and stale comments referencing
  better-sqlite3 in unified-workspace-watcher
