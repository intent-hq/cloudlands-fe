/** Canonical horizontal inset for card text and flush nested content. */
export const CARD_CONTENT_INSET_CLASS = 'px-4';

/** Split the card text inset into an outer highlight gutter and inner row padding. */
export const CARD_ROW_GUTTER_CLASS = 'mx-2 px-2';
