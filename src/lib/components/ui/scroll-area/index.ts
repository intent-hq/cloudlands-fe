import Scrollbar from './scroll-area-scrollbar.svelte';
import Root from './scroll-area.svelte';

export { scrollAreaFixtures } from './scroll-area.fixtures';
export { scrollAreaMetadata } from './scroll-area.meta';

export {
  Root,
  Scrollbar,
  //,
  Root as ScrollArea,
  Scrollbar as ScrollAreaScrollbar,
};
