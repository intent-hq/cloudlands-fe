/** Compatibility export for the canonical locale-aware compact formatter. */
// eslint-disable-next-line themis/pass-through-wrapper -- Keep the existing import path until all callers migrate to $lib/i18n/format.
export { formatCompactNumber } from '$lib/i18n/format';
