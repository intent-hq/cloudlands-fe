<!-- Tailwind scans this host so the `motion-reduce:` candidates below are
     generated for the CT contract regardless of which production callers
     currently use them. -->
<section>
  <div
    data-testid="motion-reduce-probe"
    class="animate-pulse backdrop-blur-[1px] transition-transform motion-reduce:animate-none motion-reduce:backdrop-blur-none motion-reduce:transition-none"
  >
    Probe
  </div>
</section>
