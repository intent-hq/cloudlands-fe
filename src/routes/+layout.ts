// This is an Electron app with no Node.js server, so we use SPA mode
// See: https://svelte.dev/docs/kit/single-page-apps
export const ssr = false;
export const prerender = false;
export const csr = true;
