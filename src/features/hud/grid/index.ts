/**
 * HUD center workspace-card grid — public surface. The /hud shell mounts the
 * single top-level component; everything else in this directory is internal.
 */
export { default as HudWorkspaceGrid } from './HudWorkspaceGrid.svelte';
