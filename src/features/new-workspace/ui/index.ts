/**
 * Rendering boundary for the Untitled workspace shell.
 *
 * Components present controller state and emit user intent without owning durable draft state.
 */
export { default as UntitledWorkspaceShell } from './UntitledWorkspaceShell.svelte';
export * from './types';
