# Custom runner labels for the self-hosted fleet, so actionlint's
# runner-label check recognizes them (everything else it treats as unknown).
self-hosted-runner:
  labels:
    - m1mac
    - tinybox
    # GH-hosted larger runner the route job bursts the heavy jobs to.
    - gh-linux-8x
