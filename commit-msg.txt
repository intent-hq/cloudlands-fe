fix: restore workspace-initializer persistence via daemon settings

Re-homes the deleted workspace-initializer-saga's hydration + persistence
logic as a dependency-light middleware, promoting state from localStorage to
the daemon workspaceInitializer.state setting (PROTOCOL §5.12).

Boot-time hydration reads the setting via appClient.settings.get on first
action, applies tolerant parsing (stringRecord/objectArray guards), and
dispatches hydrateWorkspaceInitializer so state.hydrated becomes true
and the home-screen repo selector defaults to the last selected repo again.

One-time migration seeds the daemon from legacy localStorage keys when the
bag is empty, preserving pre-saga-removal user data.

Each mutating action persists the updated bag via settings.update:
compactFormState, onboardingFormState, lastSelectedRepo, branchByRepo,
defaultParentPath, recentRepos, remoteSetups, lastSubmittedAgent.

Debounced onboarding form persistence mimics the deleted saga's race-based
cancellation (resetOnboarding / cancelDebounce kill the pending write).
resetOnboarding also clears the onboarding-prompt sessionStorage key.

Tests assert exact wire request shapes per FE testing rules (mock BE,
PROTOCOL-shaped responses). Middleware registered in middleware.ts.
