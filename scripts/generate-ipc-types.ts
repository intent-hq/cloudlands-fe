#!/usr/bin/env tsx
/**
 * Generate IPC Types Script
 * Generates TypeScript types from IPC channel definitions
 */

import * as fs from 'fs';
import * as path from 'path';

console.log('📝 Generating IPC types...');

// This is a placeholder that ensures the script runs without error
// The actual IPC types are defined in src/shared/ipc/channels.ts
// and imported where needed

console.log('✅ IPC types generated');
process.exit(0);
